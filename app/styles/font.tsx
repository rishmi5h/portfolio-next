import { Inter, Poppins, Rubik, Source_Sans_3 } from "next/font/google";

// define your variable fonts
const inter = Inter({ subsets: ["latin"] });
const rubik = Rubik({
    subsets: ['latin'],
    weight: "400"
});


export { inter, rubik };
